import tag from 'tag'

export default tag({

  add(params) {
    return params
  },

  remove(id) {
    return id
  },

  append(params) {
    return params
  },

  redact(item) {
    return item
  }

})
